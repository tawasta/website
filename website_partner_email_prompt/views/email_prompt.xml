<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <template id="modal_email_form" name="Email Update Modal">
        <div id="emailPromptModal" class="modal fade" tabindex="-1" role="dialog">
            <div class="modal-dialog modal-lg" role="document">
                <form id="email_update_form" action="/my/email_update" method="post" class="js_website_submit_form">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <div class="modal-content">
                        <div class="modal-header align-items-center">
                            <h4 class="modal-title">Update Your Email</h4>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span>x</span></button>
                        </div>
                        <div class="modal-body bg-light border-bottom">
                            <p>Please provide your email address to proceed:</p>
                            <div class="form-group">
                                <label for="email">Email</label>
                                <input type="email" id="emailInput" name="email" class="form-control" required="true" placeholder="Enter your email"/>
                                <div id="emailError" class="text-danger mt-2" style="display: none;">Invalid email address!</div>
                            </div>
                        </div>
                        <div class="modal-footer border-0 justify-content-between">
                            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                            <button type="submit" class="btn btn-primary">Update</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </template>
</odoo>

